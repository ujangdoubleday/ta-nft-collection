export const metadata = {
  title: "Todo List | NFT Marketplace",
  description: "Manage your tasks with our Todo List feature",
};

export default function TodosLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
